<% content_for(:title, "All Photos || Shutters" ) %>

<h1 class="jumbotron">Photos</h1>

<p><%= link_to "New Photo", new_photo_path, :class => 'btn btn-warning' %></p>

<p><% @photos.each do |photo| %>
  <%= link_to image_tag(photo.image.url(:medium)), photo_path(photo) %>
<% end %></p>


